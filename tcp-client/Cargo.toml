[package]
name = "tcp-client"
version = "0.1.0"
edition = "2021"

[dependencies]
protos = { path = "../protos" }
capnp = "0.19.6"
futures = "0.3.0"
tokio = { version = "1.0.0", features = ["net", "rt", "macros"]}
tokio-util = { version = "0.7.4", features = ["compat"] }